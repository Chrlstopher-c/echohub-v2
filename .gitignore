# Environnements
.venv/
venv/
__pycache__/
*.pyc
node_modules/
dist/
build/

# Donnees locales et modeles (jamais dans le depot)
#
# Motifs ANCRES a la racine (prefixe `/`) : sans l'ancre, `models/` excluait aussi
# `backend/models/` et `frontend/src/models/`, c'est-a-dire le domaine metier des deux cotes.
# Les deux etaient absents du depot sans que rien ne le signale.
*.gguf
*.safetensors
/models/
/data/
/chroma_data/
*.db
*.sqlite3

# Logs (reset a chaque restart, jamais versionnes)
logs/
*.log

# Secrets
.env
.env.*
!.env.example

# Outils
.claude/
.ruff_cache/
.pytest_cache/
.DS_Store

.pytest_cache/
